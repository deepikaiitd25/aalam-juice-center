[project]
name = "agent-pptx"
version = "1.0.0"
description = "Generates PowerPoint presentations from text descriptions."
requires-python = ">=3.10"
dependencies = [
    "a2a-sdk>=0.3.0",
    "openai>=1.57.0",
    "pydantic>=2.11.4",
    "python-pptx>=0.6.21"
]
