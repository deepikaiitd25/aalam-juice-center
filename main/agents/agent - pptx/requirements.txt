# Server & CLI
click>=8.1.0
uvicorn>=0.27.0
starlette>=0.36.0
python-dotenv>=1.0.0

# LLM & Models
openai>=1.12.0
pydantic>=2.6.0

# File Generation
python-pptx>=0.6.23

# Note: If your 'a2a' package is a local dependency, 
# you might need to install it locally via `pip install -e ../a2a` 
# or add its path to your PYTHONPATH.
